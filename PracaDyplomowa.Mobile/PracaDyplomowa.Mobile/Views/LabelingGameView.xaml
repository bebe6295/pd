<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:PracaDyplomowa.Mobile.Extensions"
             xmlns:controls="clr-namespace:PracaDyplomowa.Mobile.Controls"
             xmlns:vm="clr-namespace:PracaDyplomowa.Mobile.ViewModels"
             mc:Ignorable="d"
             NavigationPage.HasNavigationBar="False"
             x:Class="PracaDyplomowa.Mobile.Views.LabelingGameView">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <controls:ImageGrid Command="{Binding ChooseImageCommand}" Items="{Binding BoardItems}" />
            <Label Grid.Row="1" FontSize="Medium" HorizontalOptions="CenterAndExpand" Text="{Binding LabelToFind}" />
        </Grid>
    </ContentPage.Content>
</ContentPage>